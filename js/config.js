// --- Configuration & Constants ---
const MIN_FONT_SIZE = 9;
const MAX_FONT_SIZE = 15;
const SAFE_RENDER_LIMIT = 5000;
const MAX_FILE_SIZE = 2 * 1024 * 1024; // 2MB Limit
const CSS_VARS = {
    FONT_SIZE: '--font-size-base',
    THEME: 'data-theme'
};
